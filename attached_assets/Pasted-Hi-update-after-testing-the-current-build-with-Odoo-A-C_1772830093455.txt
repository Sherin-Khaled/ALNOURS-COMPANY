Hi — update after testing the current build with Odoo.

## A) Checkout + Payment (must be implemented properly)

Right now checkout completes without collecting required info and without payment.

1. Collect required customer data before placing order:

* Full name, email, phone
* Full address: street, city, country (+ optional postal/zip)

2. Add payment step with two options:

* **Card (Visa/Mastercard)**: allow entering card details.
* **Cash on Delivery (COD)**: place order without card.
  For card testing, please enable **test mode** so I can run a “fake/test card” payment flow safely.

3. Order submission rules:

* Don’t allow placing an order until required address fields are filled.
* After payment success (card) OR selection of COD, then proceed to create the order in Odoo.

## B) Odoo Sync: contacts + address mapping

When I add/update address in the website, it doesn’t reflect correctly in the Odoo contact.
Please ensure we update the same res.partner record with:

* street, city, zip, country_id
* phone, email, name
  And confirm which website fields map to which Odoo fields.

## C) Sales Order status (Quotation vs Confirmed)

I didn’t understand this before, so here’s what I want:

* **Default for now: create it as a “Quotation” (draft sale.order)** when checkout is done, then the company can confirm it in Odoo.
* If payment is successful (Card), we can optionally auto-confirm it later, but for now keep it Quotation to be safe.

## D) “Send Requirements / Contact Us” form -> Odoo + UI feedback

When user submits the form:

* Create a record in Odoo for tracking (preferred: CRM Lead `crm.lead`; if not available, use the best standard model you recommend).
* Show frontend status:

  * Success message if created in Odoo
  * Error message if failed (friendly error, no raw stack traces)

## E) Auth fixes (local auth)

* “Forgot password” button currently does nothing.
  Please implement reset password flow for our current local auth (email reset link + new password + success/error UI).

## F) Product page bug

* “View all” in product description does not expand the rest of the text. Please fix.

## ## G) Animated gradient mesh (white background, subtle, only in specific sections)

I want a very subtle animated “mesh/gradient” overlay, but with a **white base background** so the effect stays minimal/clean.

Requirements:

* Base background: **#FFFFFF**
* Use our primary color as a soft tint only: **#0F3D91** (with very light shades/tints, low opacity)
* Motion: very slow + subtle (no noticeable “moving wallpaper” feeling)
* DO NOT apply it site-wide. Apply it only to specific sections:

  1. Home hero section
  2. Products page header/intro section
  3. (Optional) CTA section near the footer
* Please implement it as CSS/JS (no iframes). Ideally as a reusable component so we can enable/disable it per section.


## H) Workflow question

Is there an option for me to edit the code directly inside Replit (without downloading)?
Please confirm the best workflow (branches/commits if Git is connected, or direct edits).
